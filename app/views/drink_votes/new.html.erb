<!-- Validation failure messages -->
<% if @drink_vote.errors.any? %>
  <% @drink_vote.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Drink Vote</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_drink_vote" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for user_id -->
      <div class="form-group">
        <label for="user_id" class="control-label">
          User
        </label>

        <%= select_tag(:user_id, options_from_collection_for_select(User.all, :id, :email, @drink_vote.user_id), :class => "form-control") %>
      </div>

      <!-- Label and input for drink_id -->
      <div class="form-group">
        <label for="drink_id" class="control-label">
          Drink
        </label>

        <input type="text" id="drink_id" name="drink_id" placeholder="Drink" class="form-control" value="<%= @drink_vote.drink_id %>">
      </div>

      <button class="btn btn-success">
        Create Drink Vote
      </button>
      or
      <a href="/drink_votes">Cancel</a>
    </form>
  </div>
</div>
